.button {
  background: var(--main-color);
  color: var(--white);
  border: none;
  padding: 10px 15px;
  border-radius: 4px;

  &:hover,
  &:focus {
    transition: background-color 0.3s;
    cursor: pointer;
    background: var(--main-color-light);
  }
}
